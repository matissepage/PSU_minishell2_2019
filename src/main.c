/*
** EPITECH PROJECT, 2019
** PSU_minishell2_2019
** File description:
** main.c
*/

#include "../include/minishell.h"

int main(int ac, char **av, char **env)
{
    my_exec(env);
    // binary_t *binary = NULL;
    // char *commande1 = "ls";
    // char *commande2 = "cat -e";
    // binary = add_element(binary, commande1);
    // binary = add_element(binary, commande2);
    // print_bin_tree(binary);
    return (0);
}